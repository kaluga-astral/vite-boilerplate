import { describe, it } from 'vitest';

import { createCacheService } from '@example/shared';

import { BillingRepository } from './BillingRepository';

describe('BillingRepository', () => {
  describe('', () => {
    it('', () => {
      const sut = new BillingRepository(createCacheService());
    });
  });
});
